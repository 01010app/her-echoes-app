List<Map<String, dynamic>> getVisibleWomen({
  required List<Map<String, dynamic>> todaysWomen,
  required bool isPro,
}) {
  // No recortar la lista.
  // El bloqueo visual se maneja en la UI.
  return todaysWomen;
}