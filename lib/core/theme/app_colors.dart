import 'package:flutter/material.dart';

class AppColors {
  // Core
  static const accent = Color(0xFFF70F3D);
  static const background = Color(0xFFF5F5F5);

  // Text
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white70;

  // Navigation
  static const navActiveBackground = accent;
  static const navActiveContent = Colors.white;
  static const navRestContent = Color(0xFF6D6D6D);
  static const navInactiveContent = Color(0xFFBBBBBB);

  // Surfaces
  static const surface = Colors.white;
}